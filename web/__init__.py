"""Interactive, credential-free demo of the governed core, for Cloud Run.

The governed core needs no LLM and no payment credentials, so it is safe to
expose: a visitor drives the policy engine and watches it allow or refuse, and
the signed evidence chain that results verifies itself in their browser. Nothing
here can move real money or spend an API quota.
"""
